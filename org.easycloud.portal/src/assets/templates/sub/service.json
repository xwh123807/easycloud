{
    "apiVersion": "v1",
    "kind": "Service",
    "metadata": {
        "name": "$component",
        "labels": {
            "component": "$component",
            "user": "{user}"
        }
    },
    "spec": {
        "ports": [
            {
                "name": "http",
                "port": ,
                "targetPort":
            }
        ],
        "selector": {
            "component": "$component",
            "user": "{user}"
        }
    }
}
